## OUTPUT
* innerHTML
* document.write()
* window.alert()
* console.log()
## VAR - LET - CONST
* var 
  * chỉ có var mới vừa khai báo và cập nhật lại giá trị của biến
  * hoisting
  * khai báo bên trong function => function/locally scoped, ngược lại globally scoped
* let
  * không thể khai báo lại, nhưng có thế cập nhật lại giá trị
  * hoisting
  * block scoped
* const
  * block scoped: block scope và hoisting
  * kiểu primitive (string, number, boolean, null, undefined) => không thế tái khai báo hay cập nhật lại giá trị mới cho biến
  * kiểu reference (object, array, function) => không thể khai báo hay cập nhật lại giá trị mới cho biến nhưng có thể cập nhật lại giá trị thuộc tính của biến     
## DATA TYPES
* primitive: string, number, boolean, null, undefined
* reference: object, array, function
## FUNCTION
```
let x = myFunction(4, 3);   // Function is called, return value will end up in x

function myFunction(a, b) {
  return a * b;             // Function returns the product of a and b
}
```
## OBJECTS
```
const person = {
  firstName: "John",
  lastName : "Doe",
  id       : 5566,
  fullName : function() {
    return this.firstName + " " + this.lastName;
  }
};
```
* access object
  ```
  person.lastName;
  person["lastName"];
  ```
## EVENTS
## STRING METHODS
* cắt chuối : slice (start, end), substring(start, end), substr(start, length)
* concat(): nối chuối
* trim(): bó khoảng trống đầu cuối
* padStart(), padEnd() : thêm vào đầu, cuối
* converting a String to an Array: split()
* string search: indexOf(), lastIndexOf(), startWith(), endWith()
## STRING TEMPLATE LITERALS
* Dùng back-ticks (\` \`) để
  * Quotes inside string: 
    ```
    let text = `He's often called "Johnny"`;
    ```
  * Multiline strings
    ```
    let text =
    `The quick
    brown fox
    jumps over
    the lazy dog`;
    ```
* Interpolation (nội suy)
  ```
  let firstName = "John";
  let lastName = "Doe";

  let text = `Welcome ${firstName}, ${lastName}!`;
  ```
  ```
  let price = 10;
  let VAT = 0.25;

  let total = `Total: ${(price * (1 + VAT)).toFixed(2)}`;
  ```
### NUMBER METHODS
* toFixed(): return string: làm tròn với số thập phân
* toPrecision(): return string: làm tròn tổng chữ số
* convert variable to number: Number(), parseInt(), parseFloat()
### ARRAYS
* Create
```
const array_name = [item1, item2, ...];   
const cars = [];
cars[0]= "Saab";
cars[1]= "Volvo";
cars[2]= "BMW";
const cars = new Array("Saab", "Volvo", "BMW");
```
### ARRAYS METHODS
* toString(), join(","): convert to String
* pop(): remove item cuối cùng
* push(): thêm item vào cuối cùng
* shift(): remove item đầu tiên, index bị giảm
* unshuift(: thêm item vào đầu
* delete: để lại một chỗ trống underfined trong mảng
* splice(): nối thêm item, hay remove 
  ```
  const fruits = ["Banana", "Orange", "Apple", "Mango"];
  fruits.splice(2, 0, "Lemon", "Kiwi");
  const fruits = ["Banana", "Orange", "Apple", "Mango"];
  fruits.splice(0, 1);
  ```
* concat(): merge mảng
  ```
  const myGirls = ["Cecilie", "Lone"];
  const myBoys = ["Emil", "Tobias", "Linus"];

  const myChildren = myGirls.concat(myBoys);
  ```
* slice(): cắt mảng => tạo ra một array mới
  ```
  const fruits = ["Banana", "Orange", "Lemon", "Apple", "Mango"];
  const citrus = fruits.slice(1);
  ```
### SORTING ARRAYS
### ARRAYS ITERATION
* foreach()
* map: tạo ra mảng mới bằng cách thực hiện function trên mỗi phần tử của mảng, không thay đổi mảng gốc
  ```
  const numbers1 = [45, 4, 9, 16, 25];
  const numbers2 = numbers1.map(myFunction);

  function myFunction(value) {
    return value * 2;
  }
    ```
* filter: tạo ra mảng mới với các phần tử vượt qua test
  ```
  const numbers = [45, 4, 9, 16, 25];
  const over18 = numbers.filter(myFunction);

  function myFunction(value, index, array) {
      return value > 18;
  }
  ```
* reduce: run function cho mỗi phần tử của mảng sau đó return về giá trị duy nhất (vd: tổng các phần tử trong mảng) => không thay đổi mảng gốc
  ```
  const numbers = [45, 4, 9, 16, 25];
  let sum = numbers.reduce(myFunction);

  function myFunction(total, value, index, array) {
    return total + value;
  }
  ```
* reduceRight(): giống reduce nhưng hoạt động từ sang trái
* every(): kiểm tra tất cả giá trị của mảng vượt qua test
  ```
  const numbers = [45, 4, 9, 16, 25];
  let allOver18 = numbers.every(myFunction);

  function myFunction(value, index, array) {
    return value > 18;
  }
  ```
* some(): kiểm tra một số item của array vượt qua test
  ```
  const numbers = [45, 4, 9, 16, 25];
  let someOver18 = numbers.some(myFunction);

  function myFunction(value, index, array) {
    return value > 18;
  }
  ```
## DATES
## DATES FORMATS
## DATE GET METHODS
## DATE SET METHODS
## MATH
## RANDOM
## BOOLEANS
## COMPARISONS
## CONDITIONS
## SWITCH
## FOR
* for: lặp trong block code
* for/in: lặp qua nhiều thuộc tính của object
* for/of: lặp qua giá trị của object
## FOR IN
## FOR OF
## SETS (Tập hợp các giá trị duy nhất)
```
const letters = new Set(["a","b","c"]);
```
```
// Create a Set
const letters = new Set();

// Add Values to the Set
letters.add("a");
letters.add("b");
letters.add("c");
```
| Method      | Description |
| ----------- | ----------- |
| new Set()      | Creates a new Set       |
| add()   | Adds a new element to the Set        |
| delete()   | 	Removes an element from a Set        |
| has()   | 	Returns true if a value exists in the Set        |
| forEach()   | Invokes a callback for each element in the Set        |
| values()   | 	Returns an iterator with all the values in a Set        |
| size   | Returns the number of elements in a Set        |
## MAPS (cặp key-value key có kiểu dữ liệu bất kỳ)
```
// Create a Map
const fruits = new Map([
  ["apples", 500],
  ["bananas", 300],
  ["oranges", 200]
]);
```
```
// Create a Map
const fruits = new Map();

// Set Map Values
fruits.set("apples", 500);
fruits.set("bananas", 300);
fruits.set("oranges", 200);
```
| Method      | Description |
| ----------- | ----------- |
| new Map()      | Creates a new Map      |
| set()   | 	Sets the value for a key in a Map        |
| delete()   | 	Gets the value for a key in a Map        |
| get()  | 	Removes a Map element specified by the key        |
| delete()   | Returns true if a key exists in a Map        |
| has()   | 	Returns an iterator with all the values in a Set        |
| forEach()   | 	Calls a function for each key/value pair in a Map       |
| entries()   | 	Returns an iterator with the [key, value] pairs in a Map        |
| size   | Returns the number of elements in a Map        |
## JS typeof
* primitive: string, number, boolean, null, undefined
* reference: object, array, function
* khác nhau null và undefined
  ```
  typeof undefined           // undefined
  typeof null                // object

  null === undefined         // false
  null == undefined          // true
  ```
## TYPE CONVERSION
### Converting Strings to Numbers
| Method      | Description |
| ----------- | ----------- |
| Number()      | Returns a number, converted from its argument       |
| parseFloat()   | Parses a string and returns a floating point number        |
| parseInt()   | 	Parses a string and returns an integer        |
### Converting Numbers to Strings
```
String(x)         // returns a string from a number variable x
String(123)       // returns a string from a number literal 123
String(100 + 23)  // returns a string from a number from an expression
```
```
x.toString()
(123).toString()
(100 + 23).toString()
```
### Automatic Type Conversion
```
5 + null    // returns 5         because null is converted to 0
"5" + null  // returns "5null"   because null is converted to "null"
"5" + 2     // returns "52"      because 2 is converted to "2"
"5" - 2     // returns 3         because "5" is converted to 5
"5" * "2"   // returns 10        because "5" and "2" are converted to 5 and 2
```
## REGULAR EXPRESSIONS
* là một chuỗi các ký tự tạo thành mẫu tìm kiếm (search pattern), mẫu tìm kiếm có thể sử dụng cho hoạt động tìm kiếm và thay thế văn bản
### Using search()
```
let text = "Visit W3Schools!";
let n = text.search("W3Schools");
```
### Using search with regular expression
```
let text = "Visit W3Schools";
let n = text.search(/w3schools/i);
```
### Using String replace() With a String
```
let text = "Visit Microsoft!";
let result = text.replace("Microsoft", "W3Schools");
```
### Use String replace() With a Regular Expression
```
let text = "Visit Microsoft!";
let result = text.replace(/microsoft/i, "W3Schools");
```
## ERRORS - THROW AND TRY TO CATCH
* throw: custom error
* finally: thực thi mã sau khi try và catch
### Input Validation Example
```
<!DOCTYPE html>
<html>
<body>

<p>Please input a number between 5 and 10:</p>

<input id="demo" type="text">
<button type="button" onclick="myFunction()">Test Input</button>
<p id="p01"></p>

<script>
function myFunction() {
  const message = document.getElementById("p01");
  message.innerHTML = "";
  let x = document.getElementById("demo").value;
  try {
    if(x == "") throw "empty";
    if(isNaN(x)) throw "not a number";
    x = Number(x);
    if(x < 5) throw "too low";
    if(x > 10) throw "too high";
  }
  catch(err) {
    message.innerHTML = "Input is " + err;
  }
}
</script>

</body>
</html>
```
## SCOPE (block scope, function scope, global scope)
## HOISTING
* var: đưa biến lên top và khởi tạo
  ```
  var x; // Declare x
  x = 5; // Assign 5 to x
  ```
* let và const: đưa biến lên đầu nhưng không khởi tạo (yêu cầu dữ liệu ban đầu)
  ```
  carName = "Volvo";
  let carName; //error
  ```
* JS hoist việc khai báo không hoist việc khởi tạo
  ```
  var x = 5; // Initialize x

  elem = document.getElementById("demo"); // Find an element
  elem.innerHTML = x + " " + y;           // Display x and y

  var y = 7; // Initialize y
  // y is undefined vì y đuộc khai báo chứ chưa có khởi tạo giá trị
  ```
## THIS KEY WORD
### In method: đề cập đến đối tượng sở hữu
```
const person = {
  firstName: "John",
  lastName: "Doe",
  id: 5566,
  fullName : function() {
    return this.firstName + " " + this.lastName;
  }
};
```
### Alone: chủ sở hữu là blobal object
```
let x = this;
document.getElementById("demo").innerHTML = x;
// => [object Window]
```
### in Event Handlers: đề cập đến HTML element nhận event
```
<button onclick="this.style.display='none'">Click to Remove Me!</button>
```
